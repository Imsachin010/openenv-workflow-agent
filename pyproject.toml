[project]
name = "openenv-workflow-agent"
version = "0.1.0"
description = "Workflow RL OpenEnv environment"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "pyyaml",
    "openai",
    "openenv>=0.2.0"
]

[project.scripts]
server = "server.app:app"